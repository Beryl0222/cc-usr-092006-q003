# 赛事消费归因与清算

城市赛事会联合住宿、餐饮和零售商户发放不同权益。有效窗口可能跨越午夜，退票、改期及商户迟到流水都会影响最终清算，因此原始事件和财务调整需要分开保存。

`data/programs.json` 给出两项脱敏合作方案，时间均携带明确偏移量。`program.go` 仅负责校验方案基础资料，清算规则与对外服务由项目后续模块承载。

使用 `go test ./...` 检查资料解析与时区保留行为。
